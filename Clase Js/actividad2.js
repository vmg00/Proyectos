function mayorArray(array) {
    if (array.lenght == 0) {
        return null;
    }

    else
        (array.lenght == 1); {
        return array[0];
    }

    let numMayor = array[0];
    for (let i = 1; i < array.length; i++) {
    }

    let i = 0;
    while (i < array.length) {
        console.log(arr[i]);
        i++;
    }
    let numImpares = 0;
    let b = 0;

    do {
        if (array[b]) {
            numImpares++;
        }
        i++;
    } while (b < array.length);

    console.log("La cantidad de números impares es:", numImpares);
    return numMayor;

}
